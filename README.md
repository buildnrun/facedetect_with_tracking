# facedetect_with_tracking

Face detection and tracking for continuous face detection.

# Dependency

Install opencv3

# To compile
g++ `pkg-config --cflags opencv` `pkg-config --libs opencv` facedetect.cpp -o facedetect

# To run
./facedetect
